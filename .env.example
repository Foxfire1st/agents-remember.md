# Agents Remember local path configuration
#
# This is the only MVP knob. Relative paths resolve from this .env file,
# so the default places ar-management beside this repo checkout.
#
# Copy this file to .env only when you want to customize the default.
# The .env file is intentionally ignored because it is local machine config.

AR_MANAGEMENT_ROOT=./ar-management